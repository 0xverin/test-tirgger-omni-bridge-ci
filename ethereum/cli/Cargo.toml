[package]
name = "ethereum-cli"
version = "0.1.0"
edition.workspace = true

[dependencies]
clap = {workspace = true}
alloy = { workspace = true, features = ["contract", "signer-local"]}
log = "0.4.22"
